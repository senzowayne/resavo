<nav class="navbar navbar-expand-lg blue-grey darken-4">
    <div class="container-fluid">
        {# Add a logo #}
        <img width="30px" src="https://vuejs.org/images/logo.png">
        <ul class="navbar-nav d-flex">
            {% if app.user %}
                <div class="dropstart">
                    <li class="nav-item">
                    <a class="nav-link dropdown-toggle " style="color:#c0e7f9" href="#" id="dropdownMenuButton2"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Mon compte
                    </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="nav-link" href="{{ path('new_reservation') }}">Nouvelle Réservation</a>
                            <a class="dropdown-item" href="{{ path("historique") }}">Mes Réservations</a>
                            <div class="dropdown-divider"></div>
                            {% if is_granted('ROLE_ADMIN') %}
                                <a class="nav-link" href="{{ path('app_admin_dashboard_index') }}">Administration</a>
                                <a class="nav-link" href="{{ path('config_merchant') }}">Paramètres</a>
                            {% endif %}
                            <a class="dropdown-item" href="{{ path('update_password') }}">Changer de Mot de passe</a>
                            <a class="nav-link text-danger" href="{{ path('logout') }}" tabindex="-1"
                               aria-disabled="true">Déconnexion</a>
                        </div>
                    </li>
                </div>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}">S'identifier <span
                                class="sr-only">(current)</span></a>
                </li>

                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('user_new') }}">S'inscrire <span
                                class="sr-only">(current)</span></a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
